## 3.1 示例：关节 3 的参数配置

此示例演示如何使用以下设置启用 **关节 3** 的 SoftJoint：
- **有效关节**：关节 3
- **柔顺性 (`sft`)**：50
- **角度限制 (`ang`)**：±30 度
- **阈值 (`thr`)**：10 Nm

```plaintext
softjoint_lim, j=3, sft=50, ang=30, thr=10
```